<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Lista de Usuários</title>
  </head>
  <body>
    <h1>Lista de Usuários</h1>
    <table>
      <thead>
        <tr>
          <th>Nome</th>
          <th>Email</th>
        </tr>
      </thead>
        <tbody>
          <!-----  IN traz o indice,
          ON traz o objeto, mas tem que verificar se é do prototype.. pode trazer outras coisas.. --->
          <%
            for(var i in usuarios){
            var usuario = usuarios[i];
            %>

            <tr>
              <td><a href="/detalhes.html?id=<%=usuario._id%>"><%= usuario.nome %></a></td>
              <td><%= usuario.email %></td>
            </tr>

          <% } %>
        </tbody>
    </table>
  </body>
</html>
